<%= form_for(@task) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% task.errors.full_tasks.each do |task| %>
          <li><%= task %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.label :status, 'STATUS' %>
  <%= f.select :status, [["OPEN", "OPEN"], ["CLOSE", "CLOSE"], ["PENDING", "PENDING"]] %>

  <%= f.submit '投稿' %>
<% end %>